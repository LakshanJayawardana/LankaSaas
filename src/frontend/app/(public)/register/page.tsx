'use client';import Link from 'next/link';import {FormEvent,useState} from 'react';import {api,Session,setSession} from '@/lib/api';import {useRouter} from 'next/navigation';
export default function Register(){const[error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));try{setSession(await api<Session>('/auth/register',{method:'POST',body:JSON.stringify(body)}));router.push('/dashboard')}catch(x){setError((x as Error).message)}}return <main className="auth"><section className="card"><h1>Start your business workspace</h1><p className="muted">Simple setup. You’ll be the first administrator.</p><form onSubmit={submit}>{error&&<div className="error">{error}</div>}<label>Business name<input name="businessName" required/></label><label>First name<input name="firstName" required/></label><label>Last name<input name="lastName" required/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><button className="btn">Create workspace</button></form><p className="muted">Already registered? <Link className="link" href="/login">Sign in</Link></p></section></main>}
