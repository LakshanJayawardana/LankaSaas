'use client';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';import {money} from '@/lib/invoices';
type Account={id:string;code:string;name:string;type:string;balance:number};type Line={accountCode:string;accountName:string;debit:number;credit:number};type Journal={id:string;entryDate:string;description:string;reference?:string;sourceType:string;lines:Line[]};type PL={revenue:number;expenses:number;netProfit:number;from:string;to:string};
export default function Page(){const[accounts,setAccounts]=useState<Account[]>([]),[journals,setJournals]=useState<Journal[]>([]),[pl,setPl]=useState<PL|null>(null),[error,setError]=useState('');const load=()=>Promise.all([api<Account[]>('/accounting/accounts'),api<Journal[]>('/accounting/journals'),api<PL>('/accounting/profit-loss')]).then(([a,j,p])=>{setAccounts(a);setJournals(j);setPl(p)}).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function manual(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),amount=+f.amount;try{await api('/accounting/journals',{method:'POST',body:JSON.stringify({entryDate:f.entryDate,description:f.description,reference:f.reference||null,eventId:null,lines:[{accountId:f.debitAccountId,debit:amount,credit:0},{accountId:f.creditAccountId,debit:0,credit:amount}]})});e.currentTarget.reset();await load()}catch(x){setError((x as Error).message)}}return <><div className="heading"><div><h1>Accounting</h1><p className="muted">Balanced journals, account balances and event profit in LKR.</p></div></div>{error&&<p className="error">{error}</p>}{pl&&<div className="grid"><Metric label="Revenue" value={pl.revenue}/><Metric label="Expenses" value={pl.expenses}/><Metric label="Net profit" value={pl.netProfit}/></div>}<div className="logistics-grid" style={{marginTop:20}}><div className="card"><h2>Chart of accounts</h2><table className="table"><thead><tr><th>Code</th><th>Account</th><th>Balance</th></tr></thead><tbody>{accounts.map(x=><tr key={x.id}><td>{x.code}</td><td>{x.name}<br/><span className="muted">{x.type}</span></td><td>{money(x.balance)}</td></tr>)}</tbody></table></div><div className="card"><h2>Manual journal</h2><form onSubmit={manual}><input name="entryDate" type="date" required/><input name="description" placeholder="Description" required/><input name="reference" placeholder="Reference"/><select name="debitAccountId" required defaultValue=""><option value="" disabled>Debit account</option>{accounts.map(x=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><select name="creditAccountId" required defaultValue=""><option value="" disabled>Credit account</option>{accounts.map(x=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><input name="amount" type="number" min="0.01" step="0.01" placeholder="Amount (LKR)" required/><button className="btn">Post journal</button></form></div></div><h2 style={{marginTop:28}}>Recent journals</h2><table className="table"><thead><tr><th>Date</th><th>Description</th><th>Source</th><th>Debit</th><th>Credit</th></tr></thead><tbody>{journals.map(j=><tr key={j.id}><td>{j.entryDate}</td><td>{j.description}</td><td>{j.sourceType}</td><td>{money(j.lines.reduce((s,x)=>s+x.debit,0))}</td><td>{money(j.lines.reduce((s,x)=>s+x.credit,0))}</td></tr>)}</tbody></table></>}
function Metric({label,value}:{label:string,value:number}){return <div className="card metric"><span className="muted">{label}</span><strong>{money(value)}</strong></div>}
