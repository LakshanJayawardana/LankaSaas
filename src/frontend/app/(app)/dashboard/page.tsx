'use client';import {useEffect,useState} from 'react';import {api} from '@/lib/api';
type Data={totalSales:number;totalExpenses:number;customers:number;products:number};
export default function Dashboard(){const[d,setD]=useState<Data>({totalSales:0,totalExpenses:0,customers:0,products:0});useEffect(()=>{api<Data>('/dashboard').then(setD).catch(()=>{})},[]);const cards=[['Total Sales',`LKR ${d.totalSales.toLocaleString(undefined,{minimumFractionDigits:2})}`],['Total Expenses',`LKR ${d.totalExpenses.toLocaleString(undefined,{minimumFractionDigits:2})}`],['Customers',String(d.customers)],['Products',String(d.products)]];return <><div className="heading"><div><h1>Dashboard</h1><p className="muted">A quick look at your business.</p></div></div><div className="grid">{cards.map(([a,b])=><div className="card metric" key={a}><span className="muted">{a}</span><strong>{b}</strong></div>)}</div></>}
