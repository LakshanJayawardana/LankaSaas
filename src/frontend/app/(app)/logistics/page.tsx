'use client';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';
type Resource={id:string;name:string;type:string;identifier?:string;totalQuantity:number;status:string};type Event={id:string;name:string;startsAt:string;endsAt:string};type Allocation={id:string;resourceName:string;quantity:number;status:string;returnedQuantity:number;damagedQuantity:number;missingQuantity:number};type Check={id:string;description:string;isCompleted:boolean};
export default function Page(){const[resources,setResources]=useState<Resource[]>([]),[events,setEvents]=useState<Event[]>([]),[eventId,setEventId]=useState(''),[allocations,setAllocations]=useState<Allocation[]>([]),[checklist,setChecklist]=useState<Check[]>([]),[error,setError]=useState('');const load=()=>Promise.all([api<Resource[]>('/logistics/resources'),api<Event[]>('/events')]).then(([r,e])=>{setResources(r);setEvents(e)}).catch(e=>setError(e.message));const loadEvent=(id:string)=>{setEventId(id);if(!id){setAllocations([]);setChecklist([]);return}api<{allocations:Allocation[];checklist:Check[]}>(`/events/${id}/logistics`).then(x=>{setAllocations(x.allocations);setChecklist(x.checklist)}).catch(e=>setError(e.message))};useEffect(()=>{void load()},[]);async function resource(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{await api('/logistics/resources',{method:'POST',body:JSON.stringify({...f,totalQuantity:+f.totalQuantity})});e.currentTarget.reset();await load()}catch(x){setError((x as Error).message)}}async function allocate(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{await api(`/events/${eventId}/logistics/allocations`,{method:'POST',body:JSON.stringify({resourceId:f.resourceId,quantity:+f.quantity})});await loadEvent(eventId)}catch(x){setError((x as Error).message)}}async function addCheck(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api(`/events/${eventId}/logistics/checklist`,{method:'POST',body:JSON.stringify({description:f.get('description')})});e.currentTarget.reset();await loadEvent(eventId)}async function action(path:string,body?:object){try{await api(`/events/${eventId}/logistics/${path}`,{method:'POST',body:body?JSON.stringify(body):undefined});await loadEvent(eventId)}catch(x){setError((x as Error).message)}}return <><div className="heading"><div><h1>Event logistics</h1><p className="muted">Reserve equipment and vehicles without schedule conflicts.</p></div></div>{error&&<p className="error">{error}</p>}<div className="logistics-grid"><div className="card"><h2>Resources</h2><table className="table"><tbody>{resources.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.type}</td><td>{x.totalQuantity}</td><td>{x.status}</td></tr>)}</tbody></table><form onSubmit={resource}><input name="name" placeholder="Resource name" required/><select name="type"><option>Equipment</option><option>Vehicle</option><option>Consumable</option></select><input name="identifier" placeholder="Registration / asset ID"/><input name="totalQuantity" type="number" min="1" placeholder="Quantity" required/><select name="status"><option>Available</option><option>Maintenance</option><option>Retired</option></select><button className="btn">Add resource</button></form></div><div className="card"><h2>Event plan</h2><label>Select event<select value={eventId} onChange={e=>loadEvent(e.target.value)}><option value="">Choose event</option>{events.map(x=><option value={x.id} key={x.id}>{x.name} — {new Date(x.startsAt).toLocaleDateString()}</option>)}</select></label>{eventId&&<><form className="allocation-form" onSubmit={allocate}><select name="resourceId" required><option value="">Select resource</option>{resources.filter(x=>x.status==='Available').map(x=><option value={x.id} key={x.id}>{x.name} ({x.totalQuantity})</option>)}</select><input name="quantity" type="number" min="1" placeholder="Qty" required/><button className="btn">Reserve</button></form><table className="table"><tbody>{allocations.map(x=><tr key={x.id}><td>{x.resourceName}</td><td>{x.quantity}</td><td>{x.status}</td><td>{x.status==='Reserved'&&<button className="link-button" onClick={()=>action(`allocations/${x.id}/dispatch`)}>Dispatch</button>}{x.status==='Dispatched'&&<button className="link-button" onClick={()=>{const returned=Number(prompt('Returned quantity',String(x.quantity))??x.quantity),damaged=Number(prompt('Damaged quantity','0')??0),missing=x.quantity-returned-damaged;void action(`allocations/${x.id}/return`,{returnedQuantity:returned,damagedQuantity:damaged,missingQuantity:missing})}}>Return</button>}</td></tr>)}</tbody></table><h2>Checklist</h2>{checklist.map(x=><label className="check" key={x.id}><input type="checkbox" checked={x.isCompleted} onChange={()=>action(`checklist/${x.id}/toggle`)}/>{x.description}</label>)}<form className="allocation-form" onSubmit={addCheck}><input name="description" placeholder="Checklist item" required/><button className="btn">Add</button></form></>}</div></div></>}
