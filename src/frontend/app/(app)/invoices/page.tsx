'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {api} from '@/lib/api';import {InvoiceList,money} from '@/lib/invoices';
export default function Page(){const[rows,setRows]=useState<InvoiceList[]>([]),[error,setError]=useState('');useEffect(()=>{api<InvoiceList[]>('/invoices').then(setRows).catch(e=>setError(e.message))},[]);return <><div className="heading"><div><h1>Invoices</h1><p className="muted">Create and track customer invoices.</p></div><Link className="btn" href="/invoices/new">New invoice</Link></div>{error&&<p className="error">{error}</p>}<table className="table"><thead><tr><th>Invoice</th><th>Customer</th><th>Issue date</th><th>Status</th><th>Total</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td><Link className="link" href={`/invoices/${x.id}`}>{x.invoiceNumber}</Link></td><td>{x.customerName}</td><td>{x.issueDate}</td><td><span className={`badge ${x.status.toLowerCase()}`}>{x.status}</span></td><td>{money(x.total)}</td></tr>):<tr><td colSpan={5} className="muted">No invoices yet.</td></tr>}</tbody></table></>}
